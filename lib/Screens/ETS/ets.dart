import 'package:flutter/material.dart';

class ETS extends StatefulWidget {
  const ETS({Key? key}) : super(key: key);

  @override
  _ETSState createState() => _ETSState();
}

class _ETSState extends State<ETS> {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('ETS'),
    );
  }
}
